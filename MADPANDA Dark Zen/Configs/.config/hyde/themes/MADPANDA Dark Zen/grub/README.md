# GRUB

GRUB artwork is static-first and not activated by `mad-theme-pack`.

Before any GRUB theme work, back up `/etc/default/grub`, the current GRUB theme,
and the generated config, then regenerate and reboot as a separate checkpoint.
